[project]
name = "api-testing"
version = "0.1.0"
description = "Framework de testing de APIs REST — contrato, funcional y carga"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
