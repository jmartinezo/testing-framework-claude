# Los tests E2E se añaden aquí.
# Cada fichero sigue el patrón: <flujo>.spec.ts
# Ejemplo: login.spec.ts, checkout.spec.ts, search.spec.ts
#
# Todos los tests usan el Page Object Model (POM):
# - Los selectores viven en pages/
# - Los datos viven en fixtures/test-state.json
# - Los helpers viven en helpers/
